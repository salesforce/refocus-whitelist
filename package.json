{
  "name": "refocus-whitelist",
  "version": "1.0.0",
  "description": "Utility application for Refocus's API, Real-Time and UI applications to verify that all inbound requests originate from whitelisted IP addresses.",
  "main": "src/index.js",
  "scripts": {
    "lint": "./node_modules/jscs/bin/jscs src test",
    "start": "node src/.",
    "test": "npm run lint && istanbul cover ./node_modules/mocha/bin/_mocha --report none --print none --include-pid -- -R dot --recursive"
  },
  "repository": {
    "type": "git",
    "url": "https://git.soma.salesforce.com/igoldstein/refocus-whitelist.git"
  },
  "author": "Salesforce Refocus",
  "license": "ISC",
  "dependencies": {
    "apicache": "^1.4.0",
    "debug": "^4.1.1",
    "express": "^4.16.4",
    "feature-toggles": "^1.4.0",
    "ip-regex": "^4.0.0",
    "no-kafka": "^3.4.3"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.1.4",
    "jscs": "^3.0.7",
    "sinon": "^7.3.2"
  }
}
