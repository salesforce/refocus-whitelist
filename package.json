{
  "name": "refocus-whitelist",
  "version": "1.1.0",
  "description": "Utility application for Refocus's API, Real-Time and UI applications to verify that all inbound requests originate from whitelisted IP addresses.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/.",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha --report none --print none --include-pid -- -R dot --recursive"
  },
  "repository": {
    "type": "git",
    "url": "https://git.soma.salesforce.com/igoldstein/refocus-whitelist.git"
  },
  "author": "Salesforce Refocus",
  "license": "ISC",
  "dependencies": {
    "@salesforce/refocus-logging-client": "^1.0.4",
    "apicache": "^1.4.0",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "ip-regex": "^4.1.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.2.0"
  }
}
